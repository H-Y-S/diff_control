To change the rpc interface:

---- rpcgen

1. Edit changes into ./ioc_rpc.x

2. Invoke ./run_rpcgen - this copies ioc_rpc_xdr.c, ioc_rpc_clnt.c, 
	ioc_rpc_svc.c, & ioc_rpc.h to appropriate directories

---- Server side

3. Edit ioc_rpc_xdr.c to comment out unused "register long *buf;",
	if you don't want the warning messages

4. Edit ioc_rpc_svc.c to incorporate "rpcmain" - just use the code
	in "rpcmain.c".  Note the addition of "rpcTaskInit()".  Also:
		a.  Add #include <vxWorks.h>
		b.  Add #include <rpc/rpc.h>
		c.  Remove #include <memory.h>

5. Edit rpcroutines.c to incorporate new calls.  Take code from 
	ioc_rpc_server_example.c

6. Implement new functionality in, e.g., fifo.c (server side).  Declare
	new function in ioc.h.

---- Client side

7. Edit ioc_rpc_xdr.c to comment out unused "register long *buf;",
	if you don't want the warning messages

8. Edit rpc_client.c to set up new calls, taking prototypes from
	ioc_rpc_client_example.c.  Declare public interface in "ppg_util.h"
